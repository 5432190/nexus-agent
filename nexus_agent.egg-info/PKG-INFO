Metadata-Version: 2.4
Name: nexus-agent
Version: 0.1.0
Summary: Nexus Agent autonomous commerce framework
Author: Nexus Agent Team
License: Apache-2.0
Classifier: Programming Language :: Python :: 3.11
Classifier: License :: OSI Approved :: Apache Software License
Classifier: Operating System :: OS Independent
Requires-Python: <3.13,>=3.11
Description-Content-Type: text/markdown
Requires-Dist: pydantic<3,>=2.6
Requires-Dist: httpx<0.31,>=0.26
Requires-Dist: structlog<25,>=24.0
Requires-Dist: cryptography<42,>=41.0
Requires-Dist: anyio<5.0,>=3.8
Provides-Extra: dev
Requires-Dist: pytest<9.0,>=8.0; extra == "dev"
Requires-Dist: pytest-cov<5.0,>=4.0; extra == "dev"
Requires-Dist: pytest-asyncio<0.25,>=0.22; extra == "dev"
Requires-Dist: respx<0.24,>=0.20; extra == "dev"
Requires-Dist: ruff<1.0.0,>=0.0.0; extra == "dev"
Requires-Dist: mypy<2.0,>=1.10; extra == "dev"
Requires-Dist: black<25.0,>=24.0; extra == "dev"

# nexus-agent

## 🚀 Local Development

### Fixed Wallet Generation Script
```bash
python -c "
from pathlib import Path
from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey
from cryptography.hazmat.primitives import serialization
key = Ed25519PrivateKey.generate()
pem = key.private_bytes(
    serialization.Encoding.PEM,
    serialization.PrivateFormat.PKCS8,
    serialization.NoEncryption()
)
Path.home().joinpath('.nexus/wallet.pem').write_bytes(pem)
" && chmod 600 ~/.nexus/wallet.pem
```

### ⚠️ Platform Notes

**Windows Users**: Run in WSL2 or Git Bash. Native Windows support is not currently guaranteed.
```bash
# Windows: Use WSL2
wsl
# Then follow Linux instructions above
```

### 🔧 Troubleshooting

**If `pytest` fails**:
```bash
# 1. Check Python version
python --version  # Must be 3.10+

# 2. Reinstall dependencies
pip install -e ".[test,llm]" --force-reinstall

# 3. Clear pytest cache
rm -rf .pytest_cache __pycache__
pytest tests/ -v
```
